### AUTOMATICALY r BY PTERODACTYL ###
# Only change the IP if you're using a server with multiple network interfaces, otherwise change the port only.

#-----ONESYNC----#
+set onesync legacy
set onesync legacy 1
+set onesync_enabledInfinty 1
set onesync_forceMigration true
set onesync_workaround763185 true

# enable OneSync with default configuration (required for server-side state awareness)
onesync_enabled true

# MySQL
set mysql_connection_string "server=;uid=;password=;database="
set mysql_debug_output "console"
set mysql_slow_query_warning 500

#### SET ####
# Server Data
setr sv_type "FA"

# Set your server's hostname

# Server player slot limit (must be between 1 and 32, unless using OneSync)

# Steam Web API key, if you want to use Steam authentication (https://steamcommunity.com/dev/apikey)
# -> replace "" with the key

########################################

#### SETS ####
# Server Social
sets activitypubFeed "fivem_staff@mas.to"

# Never Logs AntiCheat
#set anticheat "on"

exec "perms.cfg"
exec "resources.cfg"
exec permissions.cfg

# EssentialMode
set es_startingBank 45000
set es_startingCash 5000

# This allows players to use scripthook-based plugins such as the legacy Lambda Menu.
# Set this to 1 to allow scripthook. Do note that this does _not_ guarantee players won't be able to use external plugins.
sv_scriptHookAllowed 0

# Uncomment this and set a password to enable RCON. Make sure to change the password - it should look like #
# A comma-separated list of tags for your server.
# For example:
# - sets tags "drifting, cars, racing"
# Or:
# - sets tags "roleplay, military, tanks"
# Server Tags
sets tags "roleplay, rp, fr, qc, blg, rp strict, esx, custom cars, custom, drugs, reallife, serious, discord, economy, serious rp, seriousrp, fivem, customcars, custom scripts, realistic, life, lspd, cops, jobs, vrp, freeaccess, white, list, vehicules import, vanilla, vehicules, france, 64, slots, 64 slots, esx, roleplay, default, rp, vrp, police, lspd, cars, ems, drugs, jobs, reallife, custom, serious, discord, economy, cops, serious rp, fun, racing, mafia, drifting, seriousrp, 100 slots, 100"

# A valid locale identifier for your server's primary language.
# For example "en-US", "fr-CA", "nl-NL", "de-DE", "en-GB", "pt-BR"
# Server Language
sets locale "fr-FR"
# please DO replace root-AQ on the line ABOVE with a real language! :)

# BONUS
sets Discord "⭐・"
sets Créateur "🎓・"
sets Anti-Cheat "🔨・"
sets Anti-Ddos "🔒・"
sets Developpeur ""
sets Information "Merci rejoindre le Discord ! pour pouvoir avoir + d'info sur le serveur. 💖"

#LOGO

load_server_icon "logo.png"

# Server Banner
sets banner_detail ""
sets banner_connecting ""

add_ace group.admin command allow # allow all commands
add_ace group.admin command.quit deny # but don't allow quit
add_principal identifier.fivem:1 group.admin # add the admin to the group

# Hide player endpoints in external log output.
sv_endpointprivacy true

# enable OneSync with default configuration (required for server-side state awareness)
sets onesync legacy

# Steam Required
#sv_authMaxVariance 1
#sv_authMinTrust 5

# Hides player IP addresses from /players.json
sv_endpointprivacy true

sv_useDirectListing true

#Config
set sv_enforceGameBuild 2060
sv_hostname "VotreNomDeServeur"
rcon_password ""
sv_maxclients 128
endpoint_add_tcp "151.80.108.236:30120"
endpoint_add_udp "151.80.108.236:30120"
set sv_listingIPOverride "151.80.108.236"
sv_licenseKey CléFiveM
set steam_webApiKey SteamApiKey
add_ace group.admin command allow
test zebi